namespace forSR.components
{
    public class ForEach
    {
        public static void ForEach1()
        {
            char[] myArray = { 'V', 'I', 'N', 'I', 'C', 'I', 'U', 'S' };

            foreach (char ch in myArray)
                Console.WriteLine(ch);
        }
    }
}
